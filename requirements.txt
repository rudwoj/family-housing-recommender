# --- 필수 ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
streamlit>=1.30
plotly>=5.18
requests>=2.31
python-dotenv>=1.0

# --- 선택 ---
# folium / streamlit-folium 을 설치하면 지도가 folium 으로 자동 전환된다.
# 배포본에서는 검증된 Plotly(OpenStreetMap) 지도 경로를 쓰기 위해 의도적으로 제외한다.
# folium>=0.15
# streamlit-folium>=0.20
